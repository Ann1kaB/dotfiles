#!/bin/sh
export DISPLAY=:0

DATE=$(/bin/date +"%b %d %Y %H:%M")
VOLUME="$(amixer get Master | tail -n1 | sed -r 's/.*\[(.*)%\].*/\1/')"  
ROOT="$DATE // VOL:$VOLUME%"
#echo $ROOT

xsetroot -name "${ROOT}"
